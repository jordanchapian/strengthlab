#!/usr/bin/env node
var app = require('../app');
var defaultPort = 8110;

app.set('port', process.env.PORT || defaultPort);

var server = app.listen(app.get('port'), function() {
  console.log('Express server listening on port ' + server.address().port);
});